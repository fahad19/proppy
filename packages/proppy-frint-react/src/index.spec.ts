/* global describe, test, expect */
import * as lib from './index';

describe('proppy-frint-preact :: index', () => {
  test('is an object', () => {
    expect(typeof lib).toEqual('object');
  });
});
