/* global describe, test, expect */

import { useProppy } from './useProppy';

describe('proppy-react-hook :: useProppy', () => {
  test('is a function', () => {
    expect(typeof useProppy).toEqual('function');
  });
});
